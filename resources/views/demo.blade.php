<h1>
    Laravel 12
    <h3>
        {{--! here after $data is sent using ->with function the 'name' and 'id' keys are auto changed into $name and $id variable in blade file SO don't get confused --}}
        
        {{ $name }}
        {{ $id }}
    </h3>
</h1>